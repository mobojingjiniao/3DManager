package com.threed.manager.core.design.tokens

import androidx.compose.ui.unit.dp

/**
 * Cinematic Dark Aurora · Radius scale.
 *
 * `l` (28dp) is the signature radius — applied to all cards, modals, and FABs.
 */
object AuroraRadius {
    val s    = 8.dp
    val m    = 16.dp
    val l    = 28.dp   // signature
    val xl   = 32.dp
    val pill = 999.dp
}